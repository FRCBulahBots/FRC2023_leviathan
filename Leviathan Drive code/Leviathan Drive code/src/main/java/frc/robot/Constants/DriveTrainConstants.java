package frc.robot.Constants;

public final class DriveTrainConstants {
    // A multipler (0.0 - 1.0) to apply to all forward/reverse movement going to drive motors
    public static double FWD_REV_MULT = 1.0f;
    // A multiplier (0.0 - 10.0) to apply to all rotation movement going to drive motors.
    public static double ROT_MULT = 0.7f;
  }